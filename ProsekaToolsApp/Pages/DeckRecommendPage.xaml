<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ProsekaToolsApp.Pages.DeckRecommendPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ProsekaToolsApp.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <ScrollViewer Grid.Column="0">
            <StackPanel Spacing="12" Padding="24">
                <TextBlock FontSize="24" FontWeight="SemiBold" Text="组卡器"/>

                <TextBlock Text="地区 (region)"/>
                <ComboBox x:Name="RegionCombo" SelectedIndex="4" Width="180" SelectionChanged="RegionCombo_SelectionChanged">
                    <ComboBoxItem Content="jp"/>
                    <ComboBoxItem Content="tw"/>
                    <ComboBoxItem Content="en"/>
                    <ComboBoxItem Content="kr"/>
                    <ComboBoxItem Content="cn"/>
                </ComboBox>

                <Expander IsExpanded="False">
                    <Expander.Header>
                        <Grid Height="32" VerticalAlignment="Center">
                            <TextBlock Text="详细选项" VerticalAlignment="Center"/>
                        </Grid>
                    </Expander.Header>
                    <StackPanel Spacing="12">
                        <CheckBox x:Name="UseLatestSuiteJsonCheckBox" IsChecked="True" Content="使用最新的 suite JSON (%APPDATA%/ProsekaTools/output/owned_cards)"/>

                        <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <Button x:Name="ChooseSuiteJsonButton" Content="选择 suite JSON" Click="ChooseSuiteJsonButton_Click"/>
                            <TextBox x:Name="SuiteJsonTextBox" IsReadOnly="True" MinWidth="380" PlaceholderText="未选择文件"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <Button x:Name="ChooseMasterDataButton" Content="选择 masterdata 目录" Click="ChooseMasterDataButton_Click"/>
                            <TextBox x:Name="MasterDataTextBox" IsReadOnly="True" MinWidth="380" PlaceholderText="未选择目录"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <Button x:Name="ChooseMusicMetasButton" Content="选择 music metas JSON" Click="ChooseMusicMetasButton_Click"/>
                            <TextBox x:Name="MusicMetasTextBox" IsReadOnly="True" MinWidth="380" PlaceholderText="未选择文件"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <Button x:Name="ChoosePythonButton" Content="选择 Python" Click="ChoosePythonButton_Click"/>
                            <TextBox x:Name="PythonExeTextBox" MinWidth="380" PlaceholderText="默认: python"/>
                        </StackPanel>

                        <TextBlock Text="Live 类型"/>
                        <ComboBox x:Name="LiveTypeCombo" SelectedIndex="0" Width="220">
                            <ComboBoxItem Content="multi"/>
                            <ComboBoxItem Content="solo"/>
                            <ComboBoxItem Content="auto"/>
                            <ComboBoxItem Content="challenge"/>
                            <ComboBoxItem Content="challenge_auto"/>
                            <ComboBoxItem Content="mysekai"/>
                        </ComboBox>

                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <StackPanel>
                                <TextBlock Text="Music ID"/>
                                <TextBox x:Name="MusicIdTextBox" Width="160" Text="1"/>
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="Difficulty"/>
                                <ComboBox x:Name="MusicDiffCombo" SelectedIndex="3" Width="160">
                                    <ComboBoxItem Content="easy"/>
                                    <ComboBoxItem Content="normal"/>
                                    <ComboBoxItem Content="hard"/>
                                    <ComboBoxItem Content="expert"/>
                                    <ComboBoxItem Content="master"/>
                                    <ComboBoxItem Content="append"/>
                                </ComboBox>
                            </StackPanel>
                        </StackPanel>

                        <StackPanel>
                            <TextBlock Text="单首歌 Music ID (留空使用默认三首)"/>
                            <TextBox x:Name="ScoreSongsTextBox" PlaceholderText="留空: 226(hard), 74(expert), 448(expert)"/>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <StackPanel>
                                <TextBlock Text="Event ID (可选)"/>
                                <TextBox x:Name="EventIdTextBox" Width="160" PlaceholderText="留空则无活动"/>
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="Limit"/>
                                <TextBox x:Name="LimitTextBox" Width="160" Text="10"/>
                            </StackPanel>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <StackPanel>
                                <TextBlock Text="目标"/>
                                <ComboBox x:Name="TargetCombo" SelectedIndex="0" Width="160">
                                    <ComboBoxItem Content="score"/>
                                    <ComboBoxItem Content="power"/>
                                    <ComboBoxItem Content="skill"/>
                                    <ComboBoxItem Content="bonus"/>
                                </ComboBox>
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="算法"/>
                                <ComboBox x:Name="AlgorithmCombo" SelectedIndex="0" Width="160">
                                    <ComboBoxItem Content="ga"/>
                                    <ComboBoxItem Content="sa"/>
                                    <ComboBoxItem Content="dfs"/>
                                </ComboBox>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </Expander>

                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Button x:Name="RunButton" Content="开始组卡" Click="RunButton_Click" Height="32"/>
                    <Button x:Name="DecryptAndRunButton" Content="解密并组卡" Click="DecryptAndRunButton_Click" Height="32"/>
                    <ProgressRing x:Name="WorkingRing" IsActive="False" Width="20" Height="20"/>
                </StackPanel>

                <TextBlock x:Name="StatusText" TextWrapping="Wrap"/>
            </StackPanel>
        </ScrollViewer>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" Spacing="12" Padding="12">
                <Button Content="清空结果" Click="ClearResultsButton_Click"/>
                <TextBlock x:Name="ResultFileText" VerticalAlignment="Center"/>
            </StackPanel>

            <ListView x:Name="ResultsList" Grid.Row="1" SelectionMode="None">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:DeckRecommendEntry">
                        <StackPanel Spacing="6" Margin="0,6,0,6">
                            <ItemsControl ItemsSource="{x:Bind CardPreviews, Mode=OneWay}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <StackPanel Orientation="Horizontal" Spacing="8"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate x:DataType="local:DeckCardPreview">
                                        <StackPanel Spacing="4" Width="120">
                                            <TextBlock Text="{x:Bind CardId, Mode=OneWay}" FontSize="10"/>
                                            <Grid Width="110" Height="110">
                                                <Image Source="{x:Bind FrameImage, Mode=OneWay}" Stretch="Fill"/>
                                                <Border Margin="6">
                                                    <Image Source="{x:Bind CardImage, Mode=OneWay}" Stretch="Uniform"/>
                                                </Border>
                                                <Image Source="{x:Bind AttributeImage, Mode=OneWay}"
                                                       Width="24" Height="24"
                                                       HorizontalAlignment="Left"
                                                       VerticalAlignment="Top"
                                                       Margin="2"/>
                                                <StackPanel Orientation="Vertical"
                                                           HorizontalAlignment="Left"
                                                           VerticalAlignment="Bottom"
                                                           Margin="2">
                                                    <ItemsControl ItemsSource="{x:Bind RarityStars, Mode=OneWay}">
                                                        <ItemsControl.ItemsPanel>
                                                            <ItemsPanelTemplate>
                                                                <StackPanel Orientation="Vertical" Spacing="1"/>
                                                            </ItemsPanelTemplate>
                                                        </ItemsControl.ItemsPanel>
                                                        <ItemsControl.ItemTemplate>
                                                            <DataTemplate>
                                                                <Image Source="{Binding}" Width="12" Height="12"/>
                                                            </DataTemplate>
                                                        </ItemsControl.ItemTemplate>
                                                    </ItemsControl>
                                                </StackPanel>
                                            </Grid>
                                            <TextBlock Text="{x:Bind LoadStatus, Mode=OneWay}" FontSize="10" Foreground="Gray"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <TextBlock Text="{x:Bind SummaryLine, Mode=OneWay}" TextWrapping="Wrap"/>
                            <TextBlock Text="{x:Bind ScoreLine, Mode=OneWay}" TextWrapping="Wrap" FontSize="12" Foreground="Gray"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Page>
